const boardEl=document.getElementById('board'),statusEl=document.getElementById('status'),resetEl=document.getElementById('reset'),startEl=document.getElementById('start'),winPanel=document.getElementById('win-panel'),winTitle=document.getElementById('win-title'),winCopy=document.getElementById('win-copy');const SIZE=3;let cells=[],state=[],active=false,gameOver=false,computerTimer=null,redirectTimer=null;
function makeBoard(){boardEl.innerHTML='';cells=[];state=Array(9).fill(0);for(let i=0;i<9;i++){const r=Math.floor(i/3),c=i%3,b=document.createElement('button');b.className='cell';b.type='button';b.dataset.row=r;b.dataset.col=c;b.setAttribute('role','gridcell');b.setAttribute('aria-label',`Hex cell ${i+1}`);b.addEventListener('click',()=>playerMove(i));boardEl.appendChild(b);cells.push(b)}}
function neighbors(i){const r=Math.floor(i/3),c=i%3,out=[];for(const [dr,dc] of [[-1,0],[-1,1],[0,-1],[0,1],[1,-1],[1,0]]){const nr=r+dr,nc=c+dc;if(nr>=0&&nr<3&&nc>=0&&nc<3)out.push(nr*3+nc)}return out}
function pathFor(player){const starts=[],seen=new Set(),parent=new Map(),target=player===1?i=>i%3===2:i=>Math.floor(i/3)===2;if(player===1){for(let r=0;r<3;r++)if(state[r*3]===1)starts.push(r*3)}else{for(let c=0;c<3;c++)if(state[c]===2)starts.push(c)}const q=[...starts];starts.forEach(i=>seen.add(i));while(q.length){const i=q.shift();if(target(i)){const path=[];let cur=i;while(cur!==undefined){path.unshift(cur);cur=parent.get(cur)}return path}for(const n of neighbors(i))if(state[n]===player&&!seen.has(n)){seen.add(n);parent.set(n,i);q.push(n)}}return null}
function finish(player,path){gameOver=true;active=false;path.forEach(i=>cells[i].classList.add('winning'));if(player===1){statusEl.textContent='You won';winTitle.textContent='You made it.';winCopy.textContent='Your blue path reached from left to right. You found a winning connection.';winPanel.classList.add('show');redirectTimer=setTimeout(()=>{window.location.replace('home.html')},2200)}else{statusEl.textContent='I won';winTitle.textContent='The computer crossed.';winCopy.textContent='My yellow path reached from top to bottom. My moves are random — try again.';setTimeout(()=>winPanel.classList.add('show'),400)}}
function check(){const p=pathFor(1);if(p){finish(1,p);return true}const q=pathFor(2);if(q){finish(2,q);return true}return false}
function setCell(i,player){state[i]=player;cells[i].classList.add(player===1?'user':'computer','filled');cells[i].disabled=true}
function playerMove(i){if(!active||gameOver||state[i])return;setCell(i,1);if(check())return;statusEl.textContent='My move…';active=false;computerTimer=setTimeout(computerMove,300+Math.random()*500)}
function computerMove(){computerTimer=null;if(gameOver)return;const open=[];state.forEach((v,i)=>{if(v===0)open.push(i)});if(!open.length){return}setCell(open[Math.floor(Math.random()*open.length)],2);if(check())return;active=true;statusEl.textContent='Your move'}
function reset(){if(computerTimer)clearTimeout(computerTimer);if(redirectTimer)clearTimeout(redirectTimer);computerTimer=redirectTimer=null;gameOver=false;active=false;winPanel.classList.remove('show');makeBoard();statusEl.textContent='Your move';setTimeout(()=>{if(!gameOver)active=true},150)}
startEl.addEventListener('click',()=>{document.querySelector('.game-wrap').scrollIntoView({behavior:'smooth',block:'center'});active=true});resetEl.addEventListener('click',reset);makeBoard();setTimeout(()=>active=true,150);